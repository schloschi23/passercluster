apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
  namespace: storage
spec:
  interval: 30m
  chart:
    spec:
      chart: longhorn
      version: 1.11.1
      sourceRef:
        kind: HelmRepository
        name: longhorn
        namespace: storage
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  values:
    defaultSettings:
      defaultDataPath: /var/lib/longhorn
      createDefaultDiskWithNodeLabeled: false
      defaultReplicaCount: 2
      kubeletRootDir: /var/lib/kubelet

    # Ressourcen-Optimierung für ältere Hardware (Laptops)
    longhornManager:
      resources:
        requests:
          cpu: 100m
          memory: 256Mi

    # Ingress für dein Cloudflare-Setup
    ingress:
      enabled: true
      ingressClassName: internal # Nutzt deinen Envoy/Gateway
      host: longhorn.schloschi.com
      tls: true
      tlsSecret: schloschi-com-production-tls
